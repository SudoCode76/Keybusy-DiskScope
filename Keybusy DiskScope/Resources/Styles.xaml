<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls">

    <!-- Color tokens -->
    <!-- Keep some app-specific tokens but prefer ThemeResource for UI elements so dark theme works -->
    <SolidColorBrush x:Key="AccentBrush" Color="#0B69FF" />
    <SolidColorBrush x:Key="AccentForegroundBrush" Color="#FFFFFFFF" />

    <!-- Shadows and radii -->
    <!-- CornerRadius debe ser del tipo CornerRadius para evitar errores del compilador XAML -->
    <CornerRadius x:Key="CardCornerRadius">12</CornerRadius>
    <Thickness x:Key="CardPadding">18</Thickness>

    <!-- Card style -->
    <Style x:Key="CardBorderStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="{StaticResource CardCornerRadius}" />
        <!-- Use ThemeResource tokens so cards adapt to light/dark themes -->
        <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
        <Setter Property="Padding" Value="{StaticResource CardPadding}" />
        <Setter Property="Margin" Value="0,0,0,18" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <!-- Accent button -->
    <!-- Usar ThemeResource para heredar estilos definidos por el framework (AccentButtonStyle viene de XamlControlsResources) -->
    <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource AccentButtonStyle}">
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="Foreground" Value="{StaticResource AccentForegroundBrush}" />
        <Setter Property="MinWidth" Value="140" />
        <Setter Property="Height" Value="40" />
        <Setter Property="Padding" Value="12,0" />
    </Style>

    <!-- Pill style -->
    <Style x:Key="PillStyle" TargetType="Border">
        <Setter Property="Background" Value="{ThemeResource ControlFillColorSecondaryBrush}" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="6,2" />
    </Style>

    <Style TargetType="Page">
        <Setter Property="Background" Value="Transparent" />
    </Style>

</ResourceDictionary>
